Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.5
Flask-Migrate==4.0.5
python-dotenv==1.0.0
gunicorn==21.2.0
alembic==1.12.0

# For logging and basic analytics
requests==2.31.0
psycopg2-binary==2.9.7

# LLM Providers (for traditional chat)
openai>=1.0.0
google-generativeai
anthropic

# Google Cloud (for Vertex AI provider)
google-cloud-aiplatform
google-auth
google-auth-oauthlib

# Remove complex dependencies:
# - librosa (audio processing moved to frontend)
# - numpy (not needed for simple backend)
# - asyncio-related packages (WebSocket proxy removed) 